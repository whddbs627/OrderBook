server:
  port: 8083

spring:
  application:
    name: matching-engine

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
